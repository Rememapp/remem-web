{
    "files": [],
    "references": [
        {
            "path": "./tsconfig.app.json"
        },
        {
            "path": "./tsconfig.node.json"
        }
    ],
    "compilerOptions": {
        // See tsconfig.app.json — `baseUrl` removed for the same reason (deprecated in TS 6, unused here).
        "paths": {
            "@/*": ["./src/*"]
        }
    }
}
